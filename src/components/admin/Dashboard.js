import { useSelector } from "react-redux";


export const Dashboard = () => {
  
//   const selectAdminInfo = (state) => state.adminAuth;
//   console.log(useSelector(selectAdminInfo));
return (
    <div>
        <h1>Dashboard</h1>
    </div>
    );

};